## Test git commands

### Intro
This is a repo used for testing git commmands

### Examples

```
git commit -m "first file added" 
git fetch origin main
git log main..origin/main
```

This will show the difference in the remote branch you have just fetched

```
git rebase origin/main
```

Now you have your local commit on top of the origin/main


